# The extension's semantic version, reported by the running server alongside the git
# build stamp (get_application_info, GET /version, startup log, status dialog).
#
# Bump in the same commit as the change it describes:
#   y (minor) — a new feature/capability (new tool, new kind/op, new endpoint)
#   z (patch) — a fix or refinement of existing behavior
# x stays 0 until the tool set is considered stable.
version=0.3.2
